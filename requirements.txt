google-genai>=1.0.0      # Gemini SDK (brain + TTS)
sounddevice>=0.4.6       # microphone capture + audio playback
numpy>=1.24              # audio buffers
keyboard>=0.13.5         # push-to-talk hotkey (Windows)
pydirectinput>=1.0.4     # game-compatible key presses (DirectInput scancodes)
PyYAML>=6.0              # config file
python-dotenv>=1.0       # load API key from .env
